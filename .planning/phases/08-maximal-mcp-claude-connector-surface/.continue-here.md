---
context: phase
phase: 08-maximal-mcp-claude-connector-surface
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-08T23:22:31.011Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Verify handoff claims live — handoff, audit, and plan summaries can be stale or overbroad — grep/read current files and re-run relevant tests before acting on any "done" or "still open" claim.
- [ ] CONSTRAINT: Verify agent-created phase work — executor summaries are intent, not proof — inspect changed files and validation output before updating roadmap/state or starting dependent plans.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale handoff recitation | Prior handoffs and summaries may cite old or guessed state. | blocking | Read current source/test files and rerun targeted checks before relying on claims. |
| Trusting subagent summaries | Earlier Phase 08 work came from agents; summaries alone missed stale test expectations. | blocking | Verify actual diffs, focused tests, typecheck, and format before marking plans complete. |
| Unbatched local Vitest | Unbatched test runs exhausted local heap and obscured true failures. | advisory | Use `MS365_MCP_SKIP_CI_FLAKY=1 npm test` for CI-shaped local validation; package npm test uses `bin/run-vitest-batched.mjs`. |

<current_state>
Phase 08 is executing. Plan 08-10 rich resources is complete and validated. Plan 08-11 notifications is in progress: prompt/resource notification plumbing and one Graph webhook-backed resource update family are implemented, but the 08-11 summary, planning updates, full plan validation, and graphify refresh are not complete.
</current_state>

<completed_work>

Completed Tasks:
- Phase 08 Plan 08-09 dashboard tools: completed earlier in this session.
- Phase 08 Plan 08-10 rich resources: completed and validated.
  - Canonical `m365://` resources now cover catalog, tenant, skill, connector, and Graph-backed families.
  - `mcp://` aliases remain for compatibility.
  - Bounded Graph-backed reads and execute-tool resource links are implemented.
  - Validation recorded in `08-10-SUMMARY.md`, including `MS365_MCP_SKIP_CI_FLAKY=1 npm test` PASS (82/82 batches, CI-flaky quarantine enabled).
- Phase 08 Plan 08-11 notifications: partially implemented.
  - `src/lib/mcp-notifications/events.ts` has expanded event union and `resources/updated` metadata.
  - `src/lib/mcp-notifications/session-registry.ts` delivers `prompts/list_changed`, resource metadata, and coalesced resource updates.
  - `src/lib/mcp-notifications/coalesce.ts` coalesces by tenant/session/uri/changeType.
  - `src/lib/mcp-notifications/resource-updates.ts` centralizes canonical + legacy tenant resource update URI helpers.
  - `src/lib/delta/resource-updates.ts` maps mail message Graph webhook resources to `m365://tenant/{tenantId}/mail/messages/{id}.json`.
  - `src/lib/admin/webhooks.ts` publishes resource updates for mapped webhook notifications without logging PII fields.
  - Skill, bookmark, recipe, fact, admin memory, and audit publishers emit canonical `m365://` and legacy `mcp://` URIs.
  - `src/lib/transports/streamable-http.ts` forwards prompt list notifications through stateful sessions.
  - Test fakes now implement `sendPromptListChanged` where the interface requires it.

Validation already run for 08-11 partial work:
- `npx vitest run test/integration/notifications/resources-updated.int.test.ts test/integration/notifications/tools-list-changed.int.test.ts test/mcp-prompts.test.ts test/skills-tools.test.ts` — PASS (8), FAIL (0).
- `npx tsc --noEmit` — PASS.
- `npm run format:check` — PASS.
</completed_work>

<remaining_work>

- Finish Phase 08 Plan 08-11:
  - Verify or add tests for resource update metadata and `mapGraphNotificationToResourceUpdates` if current coverage is insufficient.
  - Note the 08-11 plan references `test/mcp-notifications.test.ts` and `test/resource-subscriptions.test.ts`, but those files were not found. Use current integration notification test suites unless creating new files is clearly needed.
  - Re-run focused notification tests plus `npx tsc --noEmit` and `npm run format:check`.
  - Create `.planning/phases/08-maximal-mcp-claude-connector-surface/08-11-SUMMARY.md`.
  - Update `.planning/ROADMAP.md` and `.planning/STATE.md` only after validation is complete.
  - Run `graphify update .`, then `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs graphify status` and confirm `stale: false` and `commit_stale: false`.
- Then continue remaining Phase 08 work:
  - Plan 08-12 completions.
  - Plan 08-13 sampling/elicitation/roots/client primitives.
  - Remaining transport parity/docs/connector smoke and Phase 08 closure.
</remaining_work>

<decisions_made>

- `m365://` is canonical; `mcp://` remains compatibility alias. This affects tests, resource docs, and notification URI fan-out.
- Prompt list changes are delivered to matching discovery sessions, not all sessions.
- Resource updates carry optional `{reason, source, changeType}` metadata in `_meta` and are coalesced by tenant/session/uri/changeType.
- One Graph-backed change family is mail messages: webhook resource paths ending in `/messages/{id}` map to durable `m365://tenant/{tenantId}/mail/messages/{encodedId}.json` URIs.
- Local CI-shaped full test validation should use `MS365_MCP_SKIP_CI_FLAKY=1 npm test`; npm test now uses the batched runner and clears inherited `NODE_OPTIONS`.
</decisions_made>

<blockers>
- None currently. Work is paused by request, not blocked.
</blockers>

## Required Reading (in order)

1. `graphify-out/GRAPH_REPORT.md` — required project map before source reads/searches; current graph built from commit `03b80eee` and needs refresh after code changes.
2. `.planning/phases/08-maximal-mcp-claude-connector-surface/08-11-PLAN.md` — current plan requirements and acceptance criteria.
3. `.planning/phases/08-maximal-mcp-claude-connector-surface/08-10-SUMMARY.md` — dependency summary and canonical resource decisions.
4. `src/lib/mcp-notifications/events.ts` — event union and publish helpers.
5. `src/lib/mcp-notifications/session-registry.ts` — tenant/session/subscription/coalescing delivery path.
6. `src/lib/mcp-notifications/coalesce.ts` — coalescing key/window behavior.
7. `src/lib/mcp-notifications/resource-updates.ts` — canonical/legacy resource URI helper.
8. `src/lib/delta/resource-updates.ts` and `src/lib/admin/webhooks.ts` — Graph webhook-to-resource update mapping.
9. `test/integration/notifications/resources-updated.int.test.ts` and `test/integration/notifications/tools-list-changed.int.test.ts` — current notification coverage.
10. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before assumption analysis.

## Critical Anti-Patterns (do NOT repeat these)

- Do not mark 08-11 complete from this handoff alone. Inspect live files and rerun validation.
- Do not start 08-12 before 08-11 has summary, planning updates, graphify refresh, and validation evidence.
- Do not reintroduce `mcp://` as canonical. It is a compatibility alias only.
- Do not run full local Vitest unbatched to reproduce CI. Use the npm batched runner.
- Do not include message subjects, file names, bodies, tokens, or secrets in resource update logs or notifications.

## Infrastructure State

- No dev server or watcher is intentionally running from this handoff.
- Working tree is dirty with Phase 08 Wave 5/Wave 6 changes, OOM runner changes, new dashboard assets, and notification work.
- Graphify exists at `graphify-out/`; it is stale relative to uncommitted code changes and must be refreshed after implementation before reporting completion.
- Placeholder scan found historical planning docs and `08-08-SUMMARY.md` with the word `placeholder`; for 08-08 this is an intentional "Known stubs" note from the foundation plan, not proof of false completion.

## Pre-Execution Critique Required

- Not applicable. This pause is mid-execution, not between design and execution.

<context>
Immediate resume mindset: finish 08-11 carefully. The implementation already passes focused tests/type/format, but plan recording and graph refresh are unfinished. The riskiest area is over-trusting summaries: verify current code and tests before advancing state. Keep tenant isolation, discovery gating, subscription filtering, and PII-free notification mapping as invariants.
</context>

<next_action>
Start with: inspect current 08-11 notification diffs and test coverage, add any missing focused tests for resource metadata/webhook mapping, then run focused notification validation, `npx tsc --noEmit`, and `npm run format:check` before writing `08-11-SUMMARY.md`.
</next_action>
