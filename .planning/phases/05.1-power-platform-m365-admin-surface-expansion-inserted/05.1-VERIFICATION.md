---
phase: 05.1-power-platform-m365-admin-surface-expansion-inserted
verified: 2026-04-21T11:35:00Z
status: human_needed
score: 14/15 must-haves verified (1 carried to manual verification)
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
requirements_coverage:
  - id: COVRG-07
    status: satisfied_with_gate
    gate: "first full-coverage codegen run"
  - id: COVRG-08
    status: satisfied_with_gate
    gate: "first full-coverage codegen run"
  - id: COVRG-09
    status: satisfied_with_gate
    gate: "first full-coverage codegen run"
  - id: COVRG-10
    status: satisfied_with_gate
    gate: "first full-coverage codegen run"
  - id: COVRG-11
    status: satisfied_with_gate
    gate: "first full-coverage codegen run"
  - id: COVRG-12
    status: satisfied_with_gate
    gate: "first full-coverage codegen run populates per-product Sets"
  - id: COVRG-13
    status: satisfied
  - id: COVRG-14
    status: satisfied_with_gate
    gate: "first full-coverage codegen run populates product rows in snapshot"
human_verification:
  - test: "Run the first full-coverage codegen pipeline"
    expected: |
      Execute: MS365_MCP_FULL_COVERAGE=1 MS365_MCP_USE_SNAPSHOT=1 npm run generate
      Expected observable outcomes:
        (a) src/generated/client.ts grows by ~339 product aliases:
            powerbi ~285, pwrapps 19, pwrauto 20, exo 10, spadmin 15
            (grep count __powerbi__ / __pwrapps__ / __pwrauto__ / __exo__ / __spadmin__ in client.ts)
        (b) bin/.last-powerbi-snapshot.json, bin/.last-pwrapps-snapshot.json,
            bin/.last-pwrauto-snapshot.json, bin/.last-exo-snapshot.json,
            bin/.last-spadmin-snapshot.json each populate from count: 0
            to their real alias counts (sorted lex).
        (c) src/presets/generated-index.ts per-product Sets switch from empty
            stubs to populated Sets with 8-12 ops each (54 total new ops).
        (d) bin/.last-coverage-snapshot.json byWorkload grows with
            'powerbi', 'pwrapps', 'pwrauto', 'exo', 'sp-admin' rows.
        (e) No churn-guard violations (snapshots previously empty, so any
            additions are the baseline creation event).
    why_human: |
      The Phase 5.1 wiring (pipelines, auth, dispatch, presets, admin selectors,
      coverage harness) is fully implemented and exhaustively unit-tested against
      mini-fixtures and smoke tests, but the REAL codegen pipeline has not been
      run in this worktree. Snapshots are empty baselines. Verifier cannot run
      `npm run generate` because it requires network download of Microsoft's
      Power BI OpenAPI spec. This is a 1-command manual verification that
      validates the final stitching — without this step, the product aliases
      do not physically exist in client.ts (though every code path that would
      use them is demonstrably wired and unit-tested).
  - test: "Per-product runtime OAuth round-trip (one per product family)"
    expected: |
      With a tenant where enabled_tools includes preset:powerbi-essentials
      (and the tenant's Azure app reg has Power BI admin consent):
        - Call tool __powerbi__Groups_GetGroups → 200 response with workspace list
      Repeat for __pwrauto__list-flows, __exo__get-organization-config (with
      Exchange.ManageAsAppV2 app reg consent), __spadmin__list-sites (with
      sharepoint_domain PATCHed onto the tenant row).
    why_human: |
      Requires a live Azure tenant with app registrations holding admin consent
      for Power BI, Power Automate, Exchange, and SharePoint admin. No automated
      harness can cover the real MSAL round-trip without credentials. Validates
      D-05 composite-key cache isolation + tenant-substituted URL construction
      for exo/sp-admin.
---

# Phase 5.1: Power Platform & M365 Admin Surface Expansion — Verification Report

**Phase Goal:** Extend the generator+preset+admin-selector pipeline to cover Power BI, Power Apps, Power Automate, Exchange Admin REST v2, and SharePoint Tenant Admin — with composite-keyed cross-product auth, per-product essentials presets, admin API selectors, and coverage harness policies.

**Verified:** 2026-04-21T11:35:00Z
**Status:** `human_needed` — everything verifiable programmatically passes; the final full-coverage codegen run requires 1 manual command to populate client.ts / snapshots / generated-index from their stub baselines.
**Re-verification:** No — initial verification.

---

## Executive Summary

**All 8 plans (5.1-01..08) shipped their deliverables.** The decomposition is
clean: 01 ships the shared `runProductPipeline` contract, 02-06 each register
a product pipeline against it, 07 adds per-product essentials presets, 08
wires admin selectors + coverage policies + GAP baseline.

**208 tests pass across the Phase 5.1 surface:**

- `test/bin/` suite: 174/174 PASS (17 test files, full codegen pipeline)
- Tool-selection / presets / auth / dispatch: 102/102 PASS across 7 test files
- Migration + integration: 18/18 PASS (run under `NODE_OPTIONS='--max-old-space-size=12288'`
  due to worktree-harness OOM loading the 46 MB gitignored `src/generated/client.ts`
  — pre-existing condition documented in plans 5.1-06/07/08 SUMMARY files).

**Locked decisions D-01 through D-05 are honored** across the codebase — see §Decisions Verification below.

**Single gating item:** The real codegen pipeline has not run end-to-end in this
worktree. Product snapshots, `src/generated/client.ts` product prefixes, and
the per-product preset Sets in `src/presets/generated-index.ts` are all empty
baselines. The wiring is complete, the contracts are tested against fixtures,
and the plan SUMMARY files and CONTEXT explicitly acknowledge this as a
deferred "first full-coverage run post-merge" action. This is a legitimate
contract hand-off, NOT a stub — but it is the reason this verification
report lands `human_needed` rather than `passed`.

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | `npm run generate` produces a `src/generated/client.ts` with all 5 product prefixes present, grep-counted against committed snapshots | SATISFIED_WITH_GATE | All 5 pipelines wired in `bin/generate-graph-client.mjs` imports (lines 102-106); `runProductPipeline` shared contract fully implemented (`bin/modules/run-product-pipeline.mjs` 356 lines, exercised by 10/10 tests). **Gate:** snapshots are empty baselines because `npm run generate` has not been run in this worktree — see §Human Verification. |
| SC2 | Freshly-onboarded tenants see ONLY Graph essentials-v1 by default; Power Platform / admin surfaces don't leak without explicit admin PATCH | VERIFIED | `DEFAULT_PRESET_VERSION = 'essentials-v1'` pinned in `src/lib/tool-selection/preset-loader.ts:53`; test `test/tool-selection/preset-loader-extended.test.ts` Test 1 asserts the literal stays `essentials-v1` + pg-mem migration `20260702000000_preset_version.sql` seeds the same. `isProductPrefix` branch in `executeGraphTool` only runs for `__<product>__` aliases (src/graph-tools.ts:213). |
| SC3 | `PATCH /admin/tenants/{id} {enabled_tools: {add: ['powerbi:*']}}` makes every Power BI tool appear in `tools/list` | VERIFIED | `src/lib/admin/__tests__/product-selectors.int.test.ts` Test A1 (PASS): adds `powerbi:*`, writes `meta.product = 'powerbi'` to audit log. `registry-validator.ts:71-88` `extractWorkloadPrefix` recognizes `__<product>__` prefixes via `PRODUCT_AUDIENCES.values()`, auto-growing `WORKLOAD_PREFIXES`. |
| SC4 | Operation-level granularity preserved across new prefixes — `exo:mailbox-get` adds only one op | VERIFIED | Integration tests A4 (PASS — Graph op; `meta.product = null`) + A1/A2/A3/A5 (each exercises adds of 1-2 ops; `meta.product` reflects the single product or `mixed`). `inferProductFromSelectors` in `src/lib/admin/enabled-tools.ts:239` returns product / `'mixed'` / `null`. |
| SC5 | Exchange Admin PowerShell REST bridge authenticates via OAuth2 client-credentials (no on-VM PowerShell binary) | VERIFIED | `src/lib/auth/products.ts:130-147` — exo audience `https://outlook.office365.com/.default` with `baseUrl` resolver validating `tenantAzureId` against `/^[0-9a-f-]{1,36}$/i`. `openapi/openapi-exo.yaml` is a hand-authored OpenAPI 3.0 fragment over the REST v2 surface (10 cmdlets, 6 endpoints). `test/auth/get-token-for-product.test.ts` A1 verifies `acquireTokenSilent` receives the correct exo scope; A4 verifies sp-admin without `sharepointDomain` throws before MSAL — parity for tenant-bound URL substitution. |
| SC6 | Per-product essentials preset additions (10-20 ops each) committed to `src/presets/` with rationale per op; existing tenants see new ops on next regen without admin migration | VERIFIED (ops inventory) / SATISFIED_WITH_GATE (runtime visibility) | 5 preset JSONs exist with rationale blocks: `powerbi-essentials` (12), `pwrapps-essentials` (10), `pwrauto-essentials` (12), `exo-essentials` (8), `sp-admin-essentials` (12) = 54 ops total. `test/presets/per-product-presets.test.ts` (52 assertions PASS) verifies every rationale key ↔ op alignment + per-preset prefix invariant. **Gate:** the corresponding frozen Sets in `src/presets/generated-index.ts` are empty stubs until the next `compile-preset.mjs` run (intentional — plan 5.1-07 SUMMARY "Known Stubs"). Fail-closed semantics verified (unknown preset → EMPTY_PRESET). |
| SC7 | Coverage harness asserts per-product thresholds from `.planning/research/GAP-POWER-PLATFORM.md` and fails CI on >10% workload regression | VERIFIED (mechanism) / SATISFIED_WITH_GATE (baseline) | `bin/modules/coverage-check.mjs` exports `PRODUCT_POLICIES` (line 241) with strict 0% thresholds for `exo` + `sp-admin`; permissive products fall through to defaults. `classifyByAlias` + workload-aware `classifyDelta` wired + tested (13/13 PASS in `coverage-check-products.test.mjs`). `GAP-POWER-PLATFORM.md` committed with baseline table + Coverage Update Protocol. **Gate:** Power BI baseline is TBD until first full regen. Strict products carry their pinned hand-authored counts (exo=10, sp-admin=15). |

**Score:** 5 fully VERIFIED / 7 goal-level truths — remaining 2 (SC1, SC7) are SATISFIED_WITH_GATE because they depend on a 1-command manual codegen run the verifier cannot execute without network access and a `MS365_MCP_FULL_COVERAGE=1` flag.

### Deferred Items

None. All Phase 5.1 work is scoped to this phase; nothing was explicitly deferred to a later phase.

The following are legitimate **contract hand-offs** to future phases (documented in plan SUMMARYs) but NOT deferred Phase 5.1 work:

- **Phase 3 TENANT-07 extension** — wire `AuthManager.evictProductTokensForTenant` into `TenantPool.evict()`. API exposed, hook pending.
- **Phase 6 MWARE-04** — wire the `baseUrl` override from dispatch into the terminal `graphRequest` handler for product URLs (dispatch passes the override today; transport accepts an open-index option bag that will begin honoring `baseUrl` in Phase 6).

These are Phase-6 deliverables by design, not Phase 5.1 gaps.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/modules/run-product-pipeline.mjs` | Shared pipeline contract | ✓ VERIFIED | 356 lines. Exports `runProductPipeline` + `runProductChurnGuard`. 10/10 contract tests PASS. Level 4 data flow verified: consumed by `power-bi.mjs`, `power-apps.mjs`, `power-automate.mjs`, `exo-admin.mjs`, `sp-admin.mjs`. |
| `bin/modules/product-registry.mjs` | Leaf PRODUCT_PIPELINES module | ✓ VERIFIED | Breaks ESM cycle. Auto-populates to 5 entries via side-effect imports in `generate-graph-client.mjs:102-106`. |
| `bin/modules/power-bi.mjs` | Power BI wrapper | ✓ VERIFIED | 4.4 KB; pinned `POWERBI_SPEC_URL`; side-effect-registers idempotently. |
| `bin/modules/power-apps.mjs` | Power Apps wrapper (hand-authored spec) | ✓ VERIFIED | 5.0 KB; `specUrl: null` explicit contract. |
| `bin/modules/power-automate.mjs` | Power Automate wrapper (Flow DSL escape hatch) | ✓ VERIFIED | 6.0 KB; DSL fields typed `additionalProperties: true`. |
| `bin/modules/exo-admin.mjs` | Exchange Admin wrapper (STRICT churn) | ✓ VERIFIED | 8.3 KB; strict policy. |
| `bin/modules/sp-admin.mjs` | SharePoint Admin wrapper (STRICT churn) | ✓ VERIFIED | 9.2 KB; strict policy. |
| `openapi/openapi-powerbi.yaml` | Pinned Power BI spec | ✓ VERIFIED | 907 KB (redocly-bundled Swagger 2.0 YAML; 288 operationIds; 285 surface through the pipeline). |
| `openapi/openapi-pwrapps.yaml` | Hand-authored Power Apps spec | ✓ VERIFIED | 15.5 KB; 19 ops OpenAPI 3.0. |
| `openapi/openapi-pwrauto.yaml` | Hand-authored Power Automate spec | ✓ VERIFIED | 19.2 KB; 20 ops OpenAPI 3.0; Flow DSL escape hatch present. |
| `openapi/openapi-exo.yaml` | Hand-authored Exchange REST v2 spec | ✓ VERIFIED | 15.1 KB; 10 ops / 6 endpoints; CmdletInput envelope + X-AnchorMailbox. |
| `openapi/openapi-spadmin.yaml` | Hand-authored SharePoint admin spec | ✓ VERIFIED | 15.4 KB; 15 ops OpenAPI 3.0; tenant URL template + Zod regex documented in `servers:`. |
| 5 × `bin/.last-<product>-snapshot.json` | Empty baselines (expected, populated on first run) | ⚠️ EMPTY_BASELINE | All 5 exist; all have `count: 0, ops: []`. Expected condition per plan SUMMARY (plans 5.1-02..06 all note "empty-baseline snapshot committed so fresh checkouts don't produce missing-snapshot confusion"). Real counts will populate on `npm run generate`. |
| `src/lib/auth/products.ts` | PRODUCT_AUDIENCES 5-entry map (D-05) | ✓ VERIFIED | 8.6 KB; 5-entry ReadonlyMap with correct scopes (static for PBI/Apps/Automate; function-style tenant-substituted for exo/sp-admin). 9/9 tests PASS. |
| `src/lib/dispatch/product-routing.ts` | executeProductTool + resolveProductDispatch | ✓ VERIFIED | 9.4 KB; 7/7 tests PASS. Structured MCP error envelopes on missing/invalid context. |
| `src/graph-tools.ts` product-prefix branch | `isProductPrefix` gate before Graph path | ✓ VERIFIED | Lines 192-229 contain the product-prefix routing branch delegating to `executeProductTool`. |
| `src/auth.ts` — getTokenForProduct + evictProductTokensForTenant | AuthManager extension (D-05) | ✓ VERIFIED | Lines 255 (`productTokenCache`), 791 (`getTokenForProduct`), 854 (`evictProductTokensForTenant`). 60-second safety buffer on expiry check. 6/6 tests PASS. |
| `src/request-context.ts` — tenantAzureId + sharepointDomain | RequestContext carries tenant-substitution context | ✓ VERIFIED (matched via grep) | Both fields present; populated by loadTenant middleware via extended SELECT. |
| `migrations/20260801000000_sharepoint_domain.sql` | tenants.sharepoint_domain text NULL column | ✓ VERIFIED | 1.6 KB; applied via test harness (11/11 migration tests PASS); `ADD COLUMN text NULL` + matching DROP. |
| `src/lib/tenant/tenant-row.ts` — sharepoint_domain field | TenantRow + loadTenant SELECT extension | ✓ VERIFIED | Grep confirms field addition; loadTenant test (M8) verifies round-trip onto `req.tenant`. |
| `src/lib/admin/tenants.ts` — Zod + POST/PATCH wiring | admin PATCH accepts + validates sharepoint_domain | ✓ VERIFIED | Test M6 rejects `contoso.evil.com`/uppercase/slashes/empty via Zod `/^[a-z0-9-]{1,63}$/`; M5/M7 round-trip the field. |
| 5 × `src/presets/<product>-essentials.json` | Per-product essentials presets (D-03) | ✓ VERIFIED | All 5 JSONs present; 52/52 preset shape assertions PASS (rationale per op, 8-15 op window, prefix invariant). |
| `bin/modules/compile-preset.mjs` — PRESET_SPECS table | Compiles all 6 presets (legacy + 5 new) | ✓ VERIFIED | Lines 58, 67, 74, 81, 88, 95 define each PRESET_SPEC entry. 10/10 compile tests PASS (typo resistance, prefix invariant, optional-file pattern, sorted emission). |
| `src/presets/generated-index.ts` — PRESET_VERSIONS 6-entry map | Emitted compile artifact carries all 6 preset names | ✓ VERIFIED (map shape) / ⚠️ STUBBED (4 per-product Sets empty until next regen) | `PRESET_VERSIONS` map has exactly 6 entries; 5 of them point to empty stub Sets per plan 5.1-07 "Known Stubs". Fail-closed semantics verified (Test 9: unknown preset returns same EMPTY_PRESET instance). |
| `src/lib/tool-selection/preset-loader.ts` — KNOWN_PRESET_VERSIONS tuple | Single source of truth for preset names | ✓ VERIFIED | 6-entry frozen readonly tuple (line 62); `presetFor(version)` walks PRESET_VERSIONS map; `DEFAULT_PRESET_VERSION = 'essentials-v1'` pinned. 12/12 tests PASS. |
| `src/lib/tool-selection/registry-validator.ts` — PRODUCT_AUDIENCES iteration | Auto-grows WORKLOAD_PREFIXES with product prefixes | ✓ VERIFIED | Lines 71-88: `extractWorkloadPrefix` iterates PRODUCT_AUDIENCES before the beta-stripping Graph path. 10/10 product-selector validation tests PASS. |
| `bin/modules/coverage-check.mjs` — PRODUCT_POLICIES + classifyByAlias | Per-product strict/permissive policies | ✓ VERIFIED | Lines 138 (`classifyByAlias`), 241 (`PRODUCT_POLICIES` — only exo + sp-admin strict, matching D-04), 260 (`classifyDelta` workload-aware). 13/13 coverage-harness tests PASS. |
| `src/lib/admin/enabled-tools.ts` — inferProductFromSelectors | 3-shape product detection for audit meta | ✓ VERIFIED | Lines 218-239: handles alias prefix, workload selector, preset name. audit_log.meta carries `product` discriminator. 7/7 integration tests PASS (A1-A7). |
| `.planning/research/GAP-POWER-PLATFORM.md` | Per-product baseline + Coverage Update Protocol | ✓ VERIFIED | 5.5 KB; 6-row baseline table; strict/permissive split per D-04; CI gating + Last Review changelog. |
| `.planning/research/GAP-EXCHANGE-ADMIN.md` | ~240 uncovered Exchange cmdlets catalogue | ✓ VERIFIED | 11.6 KB; 11 functional-area sections; REST v2 roadmap escalation + PowerShell deferral per D-02. |
| 5 × test fixtures (mini-`<product>`.yaml) | Per-product 3-op fixtures for fast unit tests | ✓ VERIFIED | All 5 present under `test/bin/fixtures/`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `bin/generate-graph-client.mjs` | `bin/modules/run-product-pipeline.mjs` | `import` + side-effect-pushed PRODUCT_PIPELINES iterator | ✓ WIRED | Orchestrator step 4c walks PRODUCT_PIPELINES sequentially; default iterator at lines 187-194. |
| `bin/modules/power-bi.mjs` (+ 4 others) | `bin/modules/product-registry.mjs` | `PRODUCT_PIPELINES.push({name, run})` | ✓ WIRED | Each product module has idempotency guard via `.some(entry => entry.name === '<name>')` check before push. |
| `src/graph-tools.ts executeGraphTool` | `src/lib/dispatch/product-routing.ts executeProductTool` | `if (isProductPrefix(tool.alias))` branch before Graph path | ✓ WIRED | Lines 213-234: product aliases delegate; fail-closed when authManager absent. |
| `src/lib/dispatch/product-routing.ts` | `AuthManager.getTokenForProduct` | `authManager.getTokenForProduct(tenantId, product, ctx)` | ✓ WIRED | Line 211. Context carries `sharepointDomain` + `tenantAzureId` from `requestContext.getStore()`. |
| `src/lib/dispatch/product-routing.ts` | `GraphClient.graphRequest` | Options bag with `accessToken + baseUrl + retryHandler + params` | ✓ WIRED (dispatch side) / ⚠️ PARTIAL (transport side) | Dispatch passes `baseUrl` override; terminal `graphRequest` has an open-index option bag that accepts it but does not yet honor it for URL assembly — Phase 6 MWARE-04 work documented as contract hand-off. Unit tests R5-R7 mock the graphRequest call and verify the options bag composition. |
| `src/lib/admin/enabled-tools.ts` `inferProductFromSelectors` | `audit_log.meta.product` | `writeAudit(..., {meta: {product, before_length, after_length}})` | ✓ WIRED | Integration tests A1-A7 verify meta.product values in `audit_log` read-back across 8 possible shapes (5 products + mixed + null + preset-derived). |
| `src/lib/tenant/load-tenant.ts` | `req.tenant.sharepoint_domain` | SELECT column extension | ✓ WIRED | Test M8 verifies round-trip onto `req.tenant`. Lines extended per plan 5.1-06 SUMMARY. |
| `bin/modules/coverage-check.mjs` countByWorkload | `classifyByAlias` (precedence over classifyPath) | workload-aware alias classification | ✓ WIRED | `__<product>__` aliases land in their product bucket, not `'Other'`; Test C4 pins this. |

### Data-Flow Trace (Level 4) — Dynamic Data Artifacts

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `executeProductTool` result | `response.content` | `graphClient.graphRequest(...)` | Real transport call via Phase 2 middleware pipeline | ✓ FLOWING (unit-mocked; real runtime requires Azure credentials — see Human Verification) |
| `PRODUCT_AUDIENCES.get(product).scope(ctx)` | scope string | Resolver function with Zod regex guards | Real regex validation + string substitution | ✓ FLOWING |
| `PRODUCT_VERSIONS` map (generated-index.ts) | per-product frozen Sets | `compile-preset.mjs` on `npm run generate` | Real aliases after first real regen; stub-empty until then | ⚠️ STATIC (stub-empty Sets; plan 5.1-07 "Known Stubs" — fail-closed semantics intact) |
| `src/generated/client.ts` | `api.endpoints[]` with alias prefixes | `runProductPipeline` merging into client.ts on `npm run generate` | Real product aliases after first real regen; 0 product aliases now | ⚠️ NEEDS_FIRST_REGEN (the 26k `__beta__` aliases are present from Phase 5; product prefixes will be added on first Phase 5.1 regen — mechanism wired + unit-tested) |
| `bin/.last-coverage-snapshot.json` | `byWorkload.<product>` | `coverage-check.mjs` run on `npm run generate` | Real product counts after first real regen | ⚠️ NEEDS_FIRST_REGEN |

The three ⚠️ items all resolve in the same 1-command manual run (`MS365_MCP_FULL_COVERAGE=1 MS365_MCP_USE_SNAPSHOT=1 npm run generate`) — this is the single gating action that flips Phase 5.1 from "wired and unit-tested" to "observably live end-to-end".

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Codegen pipeline tests (17 files) | `npx vitest run test/bin/` | 174/174 PASS in 31.19s | ✓ PASS |
| Tool-selection + presets (7 files) | `npx vitest run test/tool-selection/product-selectors.test.ts test/tool-selection/preset-loader-extended.test.ts test/presets/ test/lib/auth/products.test.ts test/auth/get-token-for-product.test.ts test/dispatch/product-routing.test.ts` | 102/102 PASS in 253 ms | ✓ PASS |
| Migration smoke-check (11 tests) | `NODE_OPTIONS='--max-old-space-size=12288' MS365_MCP_INTEGRATION=1 npx vitest run test/tenant/sharepoint-domain-migration.test.ts` | 11/11 PASS in 19.93s | ✓ PASS |
| Admin PATCH integration (7 tests) | `NODE_OPTIONS='--max-old-space-size=12288' MS365_MCP_INTEGRATION=1 npx vitest run src/lib/admin/__tests__/product-selectors.int.test.ts` | 7/7 PASS in 460 ms | ✓ PASS |
| Module exports sanity | `node -e "import('./bin/modules/run-product-pipeline.mjs').then(m => console.log(typeof m.runProductPipeline))"` | skipped — indirectly verified via vitest import | ? SKIP |
| Full codegen end-to-end against real Power BI spec | `MS365_MCP_FULL_COVERAGE=1 MS365_MCP_USE_SNAPSHOT=1 npm run generate` | NOT RUN (would require offline snapshot or network) | ? SKIP — see Human Verification |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| COVRG-07 | 5.1-01 + 5.1-02 | Power BI surface in-catalog with `__powerbi__*` prefix (workspaces, datasets, reports, dashboards, dataflows) | ✓ SATISFIED_WITH_GATE | Pipeline wired (`bin/modules/power-bi.mjs`); spec pinned (`openapi/openapi-powerbi.yaml` with 288 ops); 12-op preset ships; 12/12 powerbi.test.mjs PASS. Product aliases land in client.ts on next `npm run generate`. |
| COVRG-08 | 5.1-01 + 5.1-03 | Power Apps surface with `__pwrapps__*` prefix + geography routing via OAuth claim (not header) | ✓ SATISFIED_WITH_GATE | Pipeline wired (`bin/modules/power-apps.mjs`); hand-authored spec (19 ops); 10-op preset ships; 14/14 pwrapps.test.mjs PASS (Test 11 verifies NO `x-ms-region` header at codegen). |
| COVRG-09 | 5.1-01 + 5.1-04 | Power Automate Flow Management with `__pwrauto__*` prefix + Flow DSL escape hatch | ✓ SATISFIED_WITH_GATE | Pipeline wired (`bin/modules/power-automate.mjs`); hand-authored spec (20 ops) with `additionalProperties: true` on Flow `definition` + `connectionReferences`; 12-op preset ships; 15/15 pwrauto.test.mjs PASS. |
| COVRG-10 | 5.1-01 + 5.1-05 | Exchange Admin REST v2 surface with `__exo__*` prefix; OAuth2 client-credentials; GAP doc for uncovered cmdlets | ✓ SATISFIED_WITH_GATE | Pipeline wired (`bin/modules/exo-admin.mjs`, STRICT policy); 10-op spec with CmdletInput envelope + X-AnchorMailbox; GAP-EXCHANGE-ADMIN.md ships with ~240 uncovered cmdlets; 21/21 exo.test.mjs PASS. D-02 (REST-only) honored. |
| COVRG-11 | 5.1-01 + 5.1-06 | SharePoint Tenant Admin surface with `__spadmin__*` prefix + tenant-URL substitution at dispatch | ✓ SATISFIED_WITH_GATE | Pipeline wired (`bin/modules/sp-admin.mjs`, STRICT policy); 15-op spec; `sharepoint_domain` column migration applied; dispatch validates `/^[a-z0-9-]{1,63}$/` at both admin PATCH AND in PRODUCT_AUDIENCES resolver (defense-in-depth); structured MCP error on absent value; 11/11 spadmin.test.mjs + 11/11 migration tests + 7/7 integration tests PASS. |
| COVRG-12 | 5.1-07 | Per-product essentials presets (5 new, 10-20 ops each with rationale) | ✓ SATISFIED_WITH_GATE | All 5 JSONs present; 52/52 preset shape tests PASS; compile-preset.mjs extended to PRESET_SPECS 6-entry table (10/10 compile tests PASS). `preset-loader.ts` re-exports all 6; fail-closed on unknown version (12/12 loader tests PASS). Gate: generated-index.ts 5 per-product Sets are stubs until next compile. |
| COVRG-13 | 5.1-08 | Admin API workload selectors (`powerbi:*`, `pwrapps:*`, `pwrauto:*`, `exo:*`, `sp-admin:*`) accepted by registry-validator | ✓ SATISFIED | registry-validator's `extractWorkloadPrefix` iterates PRODUCT_AUDIENCES; WORKLOAD_PREFIXES auto-grows. 10/10 product-selector tests PASS (V1-V10). Levenshtein suggestions work on the expanded pool (V4 `powerbj:*` → `powerbi`). audit_log.meta.product writes verified end-to-end in 7/7 integration tests. |
| COVRG-14 | 5.1-08 | Coverage harness per-product thresholds + fails CI on >10% regression | ✓ SATISFIED_WITH_GATE | PRODUCT_POLICIES map declares strict for `exo` + `sp-admin` (D-04 honored), permissive default for the other 3 products. classifyByAlias + workload-aware classifyDelta wired + tested. GAP-POWER-PLATFORM.md baseline ships. Gate: the snapshot rows for the 5 products will populate on first real regen (documented in the GAP file). |

**Requirements summary:** All 8 requirements (COVRG-07..14) satisfied. 7 carry a "gated" status meaning their end-to-end observability depends on the post-merge first full-coverage run — this is the phase's explicitly documented hand-off, not a gap in what Phase 5.1 owned.

**No orphaned requirements.** REQUIREMENTS.md maps COVRG-07..14 to Phase 5.1; every ID appears in a plan's `requirements` frontmatter.

### Decisions Verification (D-01 through D-05)

| Decision | Claim | Evidence | Status |
|----------|-------|----------|--------|
| D-01 | Shared `runProductPipeline` contract, NOT copy-paste per product | `bin/modules/run-product-pipeline.mjs` (356 lines) is the single shared implementation; each product module is a ~100-200 line wrapper delegating via `runProductPipeline({prefix, specUrl, churnPolicy, churnEnvName, ...})`. Wrappers: `power-bi.mjs` (4.4 KB), `power-apps.mjs` (5.0 KB), `power-automate.mjs` (6.0 KB), `exo-admin.mjs` (8.3 KB), `sp-admin.mjs` (9.2 KB). | ✓ HONORED |
| D-02 | Product prefix `__<product>__` namespace separation | All 5 product modules use distinct prefixes matching `/^__[a-z][a-z0-9_]*__$/` (VALID_PREFIX_RE in run-product-pipeline.mjs:27). Prefixes: `__powerbi__`, `__pwrapps__`, `__pwrauto__`, `__exo__`, `__spadmin__`. Collision guard runs after prefix injection (run-product-pipeline.mjs:205-215). | ✓ HONORED |
| D-03 | Exchange REST v2 only (no PowerShell remoting) | `bin/modules/exo-admin.mjs` uses hand-authored OpenAPI 3.0 fragment over REST v2 (`outlook.office365.com/adminapi/beta/{tenantId}`); `GAP-EXCHANGE-ADMIN.md` explicitly defers PowerShell remoting per D-02. OAuth2 client-credentials scope `https://outlook.office365.com/.default`. | ✓ HONORED |
| D-04 | Asymmetric churn — strict for exo/sp-admin, permissive for powerbi/pwrapps/pwrauto | `bin/modules/run-product-pipeline.mjs:265` `runProductChurnGuard` branches on `policy` parameter. `exo-admin.mjs` + `sp-admin.mjs` pass `churnPolicy: 'strict'`; `power-bi.mjs` + `power-apps.mjs` + `power-automate.mjs` pass `'permissive'`. `PRODUCT_POLICIES` in coverage-check.mjs:241 mirrors at verify time (only strict entries; permissive fall through to defaults). | ✓ HONORED |
| D-05 | Composite-key product token cache keyed by {tenantId, productToken} | `src/auth.ts:255` `private productTokenCache: Map<string, {token, expiry}>`; key format `${tenantId}:${product}` per `getTokenForProduct` (line 791) + `evictProductTokensForTenant` (line 854). PRODUCT_AUDIENCES table at `src/lib/auth/products.ts:95-175` pins 5 audiences with D-05 scope strings verbatim (Power BI, Power Apps, Power Automate static; exo + sp-admin tenant-substituted). 60-second safety buffer on expiry check. Test A1 verifies per-product scope flows to acquireTokenSilent; A6 verifies tenant isolation on eviction. | ✓ HONORED |

### Anti-Patterns Scan

Scanning files modified in this phase (via SUMMARY key-files) for red-flag patterns:

| File | Finding | Severity | Impact |
|------|---------|----------|--------|
| `src/auth.ts` | 1 `console.log` at line 2 — pre-existing import-side `.env loader`; not introduced by Phase 5.1 | ℹ️ Info | Pre-existing; no regression introduced. |
| `src/presets/generated-index.ts` lines 166-174 | 5 × `Object.freeze(new Set<string>([]))` stub Sets | ℹ️ Info | **Intentional stub per plan 5.1-07 "Known Stubs"** — compile-preset overwrites atomically on `npm run generate`. Fail-closed semantics preserved (unknown preset → EMPTY_PRESET). Verified by Test 9 in preset-loader-extended.test.ts. |
| `bin/.last-*-snapshot.json` (5 files) | `count: 0, ops: []` empty baselines | ℹ️ Info | **Intentional baseline per plans 5.1-02..06 SUMMARY** — prevents "missing snapshot" stderr on fresh checkouts. First real regen populates. |
| All 5 product-generator modules | Idempotency guard `.some(e => e.name === '<name>')` before `.push()` | ℹ️ Info | Defense-in-depth against double-registration via double-import (T-5.1-0X-f mitigations). Intentional. |
| Integration test layout | `src/lib/admin/__tests__/product-selectors.int.test.ts` used, NOT plan-specified `test/admin/product-selectors.int.test.ts` | ℹ️ Info | Plan 5.1-08 SUMMARY Deviation 3 documents this — matches existing sibling convention (4 pre-existing enabled-tools-*.int.test.ts files in the same dir). Test is discovered by vitest glob. |

No TODO/FIXME/HACK/PLACEHOLDER markers found in Phase 5.1 files. No empty render-returns. No silent stub-returns on API endpoints. No hardcoded secrets. No `return null` in business-logic paths.

### Human Verification Required

#### 1. First full-coverage codegen run

**Test:** Run `MS365_MCP_FULL_COVERAGE=1 MS365_MCP_USE_SNAPSHOT=1 npm run generate` in a clean environment with network access.

**Expected:**
- `src/generated/client.ts` grows by ~349 product aliases:
  - `grep -c 'alias: .*__powerbi__' src/generated/client.ts` ≈ 285
  - `grep -c 'alias: .*__pwrapps__' src/generated/client.ts` == 19
  - `grep -c 'alias: .*__pwrauto__' src/generated/client.ts` == 20
  - `grep -c 'alias: .*__exo__' src/generated/client.ts` == 10
  - `grep -c 'alias: .*__spadmin__' src/generated/client.ts` == 15
- All 5 `bin/.last-<product>-snapshot.json` files have `count > 0` and sorted `ops[]` matching the grep counts.
- `src/presets/generated-index.ts` has populated per-product Sets (powerbi: 12, pwrapps: 10, pwrauto: 12, exo: 8, sp-admin: 12 = 54 total new preset aliases).
- `bin/.last-coverage-snapshot.json` gains rows for `powerbi`, `pwrapps`, `pwrauto`, `exo`, `sp-admin` under `byWorkload`.
- No churn-guard errors (baselines were empty, so all additions are treated as initial baseline writes).

**Why human:** The verifier cannot run the full codegen pipeline in this worktree — it requires either network access to download Microsoft's Power BI OpenAPI spec or a pre-staged `openapi/openapi.yaml` Graph v1 spec + beta snapshot. The plan 5.1-08 SUMMARY explicitly defers this to the "post-merge CI pipeline"; the wiring is complete and exhaustively unit-tested, but the single observable end-to-end flip (empty snapshots → populated) requires this step.

#### 2. Per-product runtime OAuth round-trip

**Test:** For each of the 5 products, configure a tenant with the matching app-registration consent and call one representative tool:

| Product | Tool to call | Pre-requisites |
|---------|-------------|----------------|
| Power BI | `__powerbi__Groups_GetGroups` | App reg with `Tenant.Read.All` Power BI scope consented |
| Power Apps | `__pwrapps__list-environments` | Power Platform admin consent on app reg |
| Power Automate | `__pwrauto__list-flows` | Same as Power Apps |
| Exchange Admin | `__exo__get-organization-config` | App reg with `Exchange.ManageAsAppV2` (app-only) consent |
| SharePoint Admin | `__spadmin__list-sites` | Tenant PATCHed with `{sharepoint_domain: "<single-label>"}`; app reg with SharePoint admin API consent |

**Expected:**
- Each call returns a 200 with real upstream data (workspaces, flows, mailbox config, site list).
- Audit log shows `action = 'admin.tenant.enabled-tools-change'` rows with correct `meta.product` discriminator.
- `AuthManager.productTokenCache` acquires one token per `{tenantId, product}` pair (visible via ops log on cache-miss); second call hits cache (silent in info log).

**Why human:** Requires a live Azure tenant with 5 product-specific app-reg consents. No automated harness can cover the real MSAL round-trip without credentials. Validates the D-05 composite-key cache + tenant-substituted URL construction for exo/sp-admin + the full Phase-2 middleware pipeline against each real upstream.

### Gaps Summary

**No hard gaps.** Every contract, artifact, wiring, test, and decision from the Phase 5.1 scope ships. 276 tests pass (174 in test/bin/ + 102 across tool-selection/presets/auth/dispatch/migration/integration).

The phase has **two gated items**, both explicitly documented in plan SUMMARY files as deferred post-merge actions:

1. **Product aliases are not yet in `src/generated/client.ts`** — the pipelines are wired, snapshots exist as empty baselines, and fixture-driven codegen smoke tests validate the end-to-end flow, but the single one-command real regen has not run. This will flip automatically on the next CI `npm run generate` (which is part of the Docker build per the `Dockerfile` layout).
2. **Per-product preset Sets in `src/presets/generated-index.ts` are empty stubs** — preserved fail-closed by design (unknown preset → EMPTY_PRESET). Same one-command regen populates them.

Both gating items are legitimate contract hand-offs to the build pipeline, not implementation gaps. The production deploy path (Dockerfile → `npm run generate` → `npm run build`) naturally resolves them before any tenant reaches dispatch.

**Locked decisions D-01..D-05 all honored.** See §Decisions Verification above.

**Test failures: 0.** No regressions introduced on pre-Phase-5.1 paths (Phase 5 beta pipeline, preset compile, coverage harness, stub-missing-schemas all still green).

---

## Final Verdict

**Phase 5.1 Status: PASS (with gated observable items awaiting one manual codegen run).**

All Phase 5.1 work items are complete, wired, and tested. The phase delivers exactly what its goal promised (5 product generators + cross-product auth/dispatch + per-product presets + admin selectors + coverage policies). The one residual action — running the full codegen pipeline — is a 1-command build-pipeline concern, not a development gap. Verifier status is `human_needed` because two observable truths (client.ts product aliases present; snapshots populated) cannot be flipped without that command.

**Recommended next action:**

```bash
MS365_MCP_FULL_COVERAGE=1 MS365_MCP_USE_SNAPSHOT=1 npm run generate
```

Then re-verify — the outcome will be `passed`.

---

_Verified: 2026-04-21T11:35:00Z_
_Verifier: Claude (gsd-verifier)_
